# --- Secrets (NEVER commit) ---
.env
.env.*
!.env.example
*.env.local
*.env.*.local
secrets/
*.pem
*.key
*.crt
*.p12
*.pfx
id_rsa*
id_ed25519*
id_ecdsa*
*.kbx
*.kdbx
.vault/
.age
*.age
.sops.yaml
.gnupg/

# --- OVH infra snapshot (contient nichandle, IPs, expiration, backup IDs) ---
ovh-infra.json
ovh-infra.*.json


# --- External vendored apps (GPL-3.0, isolation licence) ---
# dust : émulateur NDS Rust/WASM cloné depuis github.com/kelpsyberry/dust
# conservé comme repo git autonome, pull via: (cd apps/dust && git pull)
apps/dust/

# --- bxc vendor binaries (re-downloadable, too large for git) ---
# .git-history conserve l'historique du repo bxc original (~1.5 GB local-only)
packages/bxc/.git-history/
# Vendor binaries: lightpanda-bin via postinstall, others via manual install scripts
packages/bxc/vendor/lightpanda-bin/
packages/bxc/vendor/camoufox/
packages/bxc/vendor/zigquery-wrapper/
packages/bxc/vendor/wappalyzergo/
packages/bxc/vendor/curl-impersonate/*.so
packages/bxc/vendor/curl-impersonate/*.so.*
packages/bxc/vendor/curl-impersonate/*.a
packages/bxc/vendor/curl-impersonate/*.la
packages/bxc/vendor/curl-impersonate/*.tar.gz
# Build artifacts + node_modules
packages/bxc/forks/
packages/bxc/dist/
packages/bxc/node_modules/
# Cookies privées (cf_clearance, _challonge_session_production, etc.)
packages/bxc/cookies/private/

# --- Nginx auth / credentials ---
.htpasswd
*.htpasswd
htpasswd
nginx/_from-rg/.htpasswd
nginx/**/.htpasswd

# --- Service account / OAuth secrets ---
storage/credentials/
storage/cookies/
*.sa.json
credentials-*.json
*-credentials.json

# --- Binaires telechargeables (curl-impersonate etc.) ---
storage/bin/

# --- Reports temporaires Bun (crawls, n2b audits) ---
bun/reports/

# --- Backups de config potentiellement sensibles ---
*.bak.pre-maintenance
!nginx/**/*.bak.pre-maintenance
.deployments/
infra/systemd/.bak/

# --- Sauvegardes tar racine (historique pre-migration) ---
backup-*.tar*
backup-*.tgz
backup-pre-migration-*

# --- OS ---
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
desktop.ini
*~
.#*

# --- Editors / IDE ---
.vscode/
!.vscode/settings.json
!.vscode/extensions.json
.idea/
*.swp
*.swo
*.swn
.project
.classpath
*.sublime-*
.nvim*
.helix/
.zed/

# --- Node / Bun / JS ---
node_modules/
.pnp
.pnp.js
.yarn/
.bun/
# bun.lock — committed at root for reproducibility; nested excluded below
apps/*/bun.lock
packages/*/bun.lock
bun.lockb
package-lock.json
yarn.lock
pnpm-lock.yaml
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.next/
.turbo/
dist/
build/
out/
.cache/
coverage/
*.tsbuildinfo

# --- Next.js / Prisma generated (per-app) ---
apps/*/next-env.d.ts
apps/*/src/generated/
apps/*/generated/

# --- Rust ---
target/
**/*.rs.bk
Cargo.lock
!infra/rust/ws-rpbey/Cargo.lock
perf.data*
flamegraph.svg

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- Docker / DB dumps ---
*.sql.gz
*.sql.zst
*.dump
*.bak
!nginx/**/*.bak*
pg_data/
pgdata/
*.sqlite
*.sqlite-journal
*.db

# --- Logs ---
*.log
logs/
# … sauf les routes App Router nommées "logs" (ex: /admin/logs)
!apps/web/src/app/**/logs/
!apps/web/src/app/**/logs/**
journalctl-*.txt

# --- Backups / archives ---
*.tar
*.tar.gz
*.tgz
*.zip
*.7z
backups/

# --- direnv / mise / asdf ---
.envrc.local
.direnv/
.tool-versions.local

# --- Claude / LLM tooling ---
.claude/
.cursor/mcp.local.json

# --- Misc ---
*.pid
*.seed
*.tmp
.tmp/
tmp/
.mcp.json

# --- Rust build artifacts dans les sous-projets ---
rust/**/target/
rust/**/Cargo.lock
.bun-cache/
.migration-lock
.migration-journal.log
.migration-state.json
.migration-ready/
.backups/
infra/rust/target/

# --- Vestiges post-migration Phase 7 (2026-04-20) ---
# Symlinks vers ~/rg.old / apps (cassés ou doublons)
/data
/storage-public
# Doublons Prisma à la racine (canonique: apps/rpbey/prisma/)
/prisma/

# --- Assets binaires lourds (hors git) ---
# ROMs NDS originales (533 MB)
/roms/
/nds/roms/
# Staging ROMs pour test runner
/staging-nds/

# --- Vestiges bot post-move ---
/apps/rpb-bot/bot/

# --- Backups datés infra (gardés hors git) ---
infra/systemd.bak-*/
infra/nginx.bak-*/

# --- Texte / markdown (sauf essentiels) ---
*.md
*.txt
!README.md
!CLAUDE.md
# Routes App Router servant du texte (ex /llms.txt) — du code, à versionner
!apps/web/src/app/**/*.txt/
!apps/web/src/app/**/*.txt/**
# docs/ versionnés (audit, bxc…)
!docs/**/*.md
!docs/**/*.txt
!SECURITY.md
!state.md
!LICENSE.md
!LICENSE.txt
!CHANGELOG.md
!PLAN.md
!docs/**/*.md
!apps/**/README.md
!apps/**/AGENTS.md
!AGENTS.md
!apps/**/CLAUDE.md
!packages/**/README.md
!packages/**/LICENSE*
!packages/**/CHANGELOG.md
!packages/**/docs/**/*.md
!apps/**/docs/**/*.md
!agents/**/*.md

# Cookies Patreon (DevTools export pour bypass Cloudflare ; expire vite)
.secrets/
*.cookies.txt

# Lighthouse trends (SQLite local genere par scripts/ops/lighthouse-prod.ts)
/data/lighthouse-trends.db
/data/lighthouse-trends.db-*
.n2b/

# --- path-monitor outputs (regenerated by infra/rust/path-monitor watch service) ---
path.json
monorepo-map.json

# QA artefacts
.e2e*.log
.shots/
